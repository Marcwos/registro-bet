services:
  db:
    image: postgres:17-alpine
    container_name: registro_bet_db
    restart: unless-stopped
    ports:
      - "5433:5432"
    env_file:
      - ./backend/.env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
